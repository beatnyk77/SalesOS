import { WhatsAppClient } from './WhatsAppClient'

export const metadata = {
  title: 'WhatsApp | SalesOS',
  description: 'Review and approve WhatsApp follow-ups.',
}

export default function WhatsAppPage() {
  return <WhatsAppClient />
}
