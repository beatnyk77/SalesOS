import { ClosingClient } from './ClosingClient'

export const metadata = {
  title: 'Closing & Contracts | SalesOS',
  description: 'Manage final deal negotiations and contract generation.',
}

export default function ClosingPage() {
  return <ClosingClient />
}
